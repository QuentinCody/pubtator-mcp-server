{
  "name": "pubtator-mcp-server",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev --port 8854",
    "start": "wrangler dev",
    "cf-typegen": "wrangler types",
    "lint": "biome lint --diagnostic-level=error src/",
    "type-check": "npm run cf-typegen && tsc --noEmit",
    "test": "npm run type-check"
  },
  "dependencies": {
    "@bio-mcp/shared": "workspace:*",
    "agents": "0.20.1",
    "zod": "^4.3.6",
    "@modelcontextprotocol/server": "2.0.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.1.3",
    "@types/node": "^24.2.1",
    "typescript": "5.8.3",
    "wrangler": "^4.56.0"
  },
  "workspaces": [
    "packages/*"
  ]
}
